CREATE TABLE element (
    key varchar,
    description text,
    domain text,
    url text,
    selector text,
    timestamp timestamp without time zone,
    date date,
    hour varchar,
    minute varchar,
    second varchar
);